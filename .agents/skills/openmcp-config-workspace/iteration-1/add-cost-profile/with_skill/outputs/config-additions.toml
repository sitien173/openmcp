# New cheaper implementation target.
[[targets]]
id = "forge-mini"
backend = "codex"
model = "gpt-5-mini"
capabilities = ["code"]

# Profiles map workflows directly to target IDs.
[profiles.cost]
implement = "forge-mini"
review = "sentinel-primary"
consult = "sage-primary"
