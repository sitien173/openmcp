interface:
  display_name: "OpenMCP Config"
  short_description: "Edit OpenMCP targets and profiles safely"
  default_prompt: "Use $openmcp-config to add or change an OpenMCP target, profile, overlay, or task guide, then reload the daemon catalog when applicable."

dependencies:
  tools: []
