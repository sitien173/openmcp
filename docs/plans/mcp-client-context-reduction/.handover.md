---
status: ACTIVE
topic: Reduce MCP client context cost from the jobs resource, resource surface, and coordinator skill
current_phase: 0
next_action: "Execute Phase 1"
project_id: null
phase_base: null
context_key: mcp-client-context-reduction
backlog_ids: []
guidance:
  implement: { workflow: implement, profile: null }
  consult: { workflow: consult, profile: null }
  review: { workflow: review, profile: null }
job_refs: { phase: 0, latest_consult: null, latest_implementation: null, latest_review: null }
read_first:
  - docs/plans/mcp-client-context-reduction/DESIGN.md
  - docs/plans/mcp-client-context-reduction/PLAN.md
  - src/openmcp/server.py
  - src/openmcp/models.py
  - tests/test_server.py
completed_tasks: []
completed_phases: []
---

Phase 1 runs in `/home/ngosi/projects/openmcp`. Phases 2 and 3 run in
`/home/ngosi/projects/superpowers-ccg`, a separate Git root. If routed, that
repository needs its own project registration and its own `project_id`; the
field above tracks the OpenMCP project only. Both later phases change only
Markdown, a Bash test, and JSON version fields, which match the
skip-coordination rule, so executing them directly is acceptable.

Phase 2 must carry the three repointed contract assertions. `tests/run.sh` pins
`Accepted debt must never be lost`, `Blocking findings are never filed`, and
`Filing a row never creates its own commit` to `SKILL.md`, and all three move
into `references/review.md`. Splitting them into a later phase leaves the tree
red.

The provider-identity guard in `tests/test-contracts.sh` greps `skills/` for
`\bcodex\b` and `\bagy\b`. The `context_init` documentation added in Phase 2
must name no backend, flag, or generated filename.

Events were measured and deliberately left out of scope. At a maximum of 25 per
job and 2 to 336 bytes each, `openmcp://jobs/{job_id}/events` costs about 2 KB.
It is removed for resource-surface reasons, not payload reasons.
