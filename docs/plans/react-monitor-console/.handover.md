---
status: ACTIVE
topic: React+Vite Flowforge monitor & debug console replacing Alpine dashboard
current_phase: 1
next_action: "Execute Phase 1"
project_id: ad9a3a2d-4583-4ac7-a954-ba21c7162055
phase_base: null
context_prefix: react-monitor-console/phase-01
guidance:
  implement: { workflow: implement, profile: google_flash_impl }
  consult: { workflow: consult, profile: consult }
  review: { workflow: review, profile: review }
job_refs: { phase: 1, latest_consult: null, latest_implementation: null, latest_review: null }
read_first: [docs/plans/react-monitor-console/PLAN.md, docs/plans/react-monitor-console/DESIGN.md]
completed_tasks: []
completed_phases: []
---

# Handover: React Monitor & Debug Console

Frontend-only rewrite. React 19 + Vite + TS, Flowforge product shell, monitor +
debug only (no config/task-guide editing). Committed build served by the existing
FastMCP `/dashboard` route; no backend Python changes.

Five phases in PLAN.md:
1. Scaffold + build integration + Flowforge shell
2. Typed data layer + polling
3. Monitor views (Overview, Projects, Targets, Profiles)
4. Jobs view + debug inspector + tests
5. Cutover + cleanup + end-to-end verification

All profiles resolve at execution. Repo root was dirty with unknown files
(`.claude/.headroom_wrap_marker.json`, `.mcp.json`) at authoring time — not
touched.
